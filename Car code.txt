<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Simple Car Display</title>
  <style>
    :root{font-family:system-ui,Segoe UI,Roboto,Arial,sans-serif}
    body{display:flex;align-items:center;justify-content:center;min-height:100vh;margin:0;background:linear-gradient(180deg,#e9f3ff,#ffffff)}
    .card{width:90vw;max-width:720px;padding:24px;border-radius:12px;box-shadow:0 8px 30px rgba(20,40,80,0.12);background:linear-gradient(180deg,rgba(255,255,255,0.85),rgba(250,250,250,0.9));text-align:center}
    h1{margin:0 0 12px;font-size:20px;color:#123}
    p{margin:0 0 18px;color:#335}
    /* center the svg and give it a gentle hover lift */
    .car-wrap{display:flex;align-items:center;justify-content:center}
    svg{width:100%;max-width:560px;transition:transform .3s ease,filter .3s ease}
    svg:hover{transform:translateY(-6px);filter:hue-rotate(6deg) saturate(1.05)}
    /* small responsive tweaks */
    @media (max-width:420px){.card{padding:16px} h1{font-size:18px}}
  </style>
</head>
<body>
  <div class="card">
    <h1>Simple Car (Inline SVG)</h1>
    <p>Scaled, responsive SVG of a car — copy this HTML into a file and open in your browser.</p>
    <div class="car-wrap">
      <!-- Simple, single-file car drawn with SVG shapes -->
      <svg viewBox="0 0 800 400" xmlns="http://www.w3.org/2000/svg" aria-label="Stylized car illustration">
        <!-- road -->
        <rect x="0" y="320" width="800" height="80" fill="#2b2b2b" />
        <rect x="0" y="352" width="800" height="8" fill="#f3f3f3" opacity="0.12" />

        <!-- car body -->
        <g id="car">
          <!-- lower body -->
          <rect x="110" y="200" rx="20" ry="20" width="580" height="80" fill="#0077c8" />
          <!-- cabin -->
          <path d="M170 200 L300 140 L520 140 L650 200 Z" fill="#0093ff" />
          <!-- windows -->
          <path d="M310 150 L480 150 L560 200 L300 200 Z" fill="#cfeeff" opacity="0.95" />

          <!-- bumper / trim -->
          <rect x="110" y="248" width="580" height="12" rx="6" fill="#0a4570" opacity="0.5" />

          <!-- wheels -->
          <g transform="translate(0,0)">
            <circle cx="230" cy="300" r="48" fill="#111" />
            <circle cx="230" cy="300" r="26" fill="#bfbfbf" />
            <circle cx="560" cy="300" r="48" fill="#111" />
            <circle cx="560" cy="300" r="26" fill="#bfbfbf" />
          </g>

          <!-- headlights -->
          <ellipse cx="680" cy="230" rx="18" ry="12" fill="#ffd87a" opacity="0.95" />
          <ellipse cx="120" cy="230" rx="12" ry="8" fill="#a6d4ff" opacity="0.7" />

          <!-- simple shadow under car -->
          <ellipse cx="350" cy="320" rx="220" ry="28" fill="#000" opacity="0.12" />
        </g>

        <!-- simple decorative sun -->
        <g transform="translate(60,60)">
          <circle cx="0" cy="0" r="36" fill="#ffd24d" opacity="0.95" />
        </g>
      </svg>
    </div>
  </div>
</body>
</html>
